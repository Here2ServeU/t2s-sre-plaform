# Monitoring: Prometheus
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: backend-monitor
  namespace: t2s-app
spec:
  selector:
    matchLabels:
      app: backend
  endpoints:
  - port: http
    interval: 10s
